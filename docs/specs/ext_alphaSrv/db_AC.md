# Анализ структуры БД для разработки `ext_alphaSrv`

## 1. Бизнес-термины ➔ Сущности БД

| Термин из Концепции | Сущность в БД АльфаЦЕНТР | Ключевые таблицы / поля |
| :--- | :--- | :--- |
| Свойства опроса ТУ/УСПД | Общие свойства опроса ТУ/УСПД | `plan_opr2` |
| Свойства ТО | Свойства ТО | `place2` |
| Конфигурация опроса (Этап 2) | Расширенные параметры опроса | `param_oprsh2`, `param_oprkontr2`, `place2` |


| Привязка ТУ к УСПД | Группы контроллеров | `gr_kon` (`n_kon`, `n_sh`) |
| `last_data` (Время последних данных) | Состояние опроса | `amr_comm_state` (`n`, `tm` / `logtm`) |

---

### 2.3. Таблицы для ЗАПИСИ (Обновление параметров на Этапе 1)

**`place2`** (Свойства ТО):

- `id`,  NUMBER: ID ТО.
- `state`, NUMBER (0/1): если 0: данный данная ТО исключается из дальнейшего анализа.
- `connectwait`, NUMBER: ожидать соединения (сек.). Необходимое значение: **?????????????????????????**.
- `kol_pop`, NUMBER: количество попыток соединиться. Необходимое значение в зависимости от режима:
  - econom: `3`
  - normal: если есть и валиден JSON:`connection_retries`: `connection_retries`, иначе: `5`.

**`plan_opr2`** (Общие свойства опроса ТУ/УСПД):

- `place_id`, NUMBER: ID ТО.
- `n_sh`, NUMBER: ID ТУ/УСПД.
- `interv`, VARCHAR(4) (30m, 1h - 12h, 1d, 10d, 1n; где h - час, m - минта, d - день, n - месяц): интервал опроса.
Необходимое значение: если значение <= `1h`: не меняется, иначе: если есть и валиден JSON:`interval`: `interval`, иначе: `8h`.
- `kol_pop_max`, NUMBER: количество повторов опроса в интервале при неудаче.
Необходимое значение в зависимости от режима:
  - econom: `1`
  - normal: если `interv` в часах (h) и > 1: `interv` - 1, иначе: `1`.
- `state`, NUMBER (0/1): если 0: данная ТУ исключается из дальнейшего анализа.
- `dorder`, NUMBER: порядковый номер в списке опроса. Необходимое значение: `1`.
- `pause_retry`, NUMBER: пауза перед повтором опроса (сек.). Необходимое значение: `interv`(сек.) / (`kol_pop_max` + 1).

---
`gr_kon` (Привязка ТУ к контроллеру):
    - `n_kon` — ID УСПД
    -   `n_sh` — ID ТУ.
Роль: Главная таблица для получения списка всех ТУ, принадлежащих конкретному УСПД (`usp_ids`).

### 2.2. Таблицы для ЧТЕНИЯ (Анализ `last_data` и текущих настроек)

    *   `amr_comm_state` (Состояние опроса ТУ)
    *   `n` — предположительно ID ТУ (`n_sh`).
    *   `tm` (или `logtm` / `stm`) — временная метка последнего успешного опроса/получения данных. Это и есть наш **`last_data`** из Концепции.
    
### 4. Критические вопросы к предметному эксперту (Перед стартом разработки)

Поскольку мы опираемся на новые таблицы, разработчику **необходимо получить 100% подтверждение** по следующим полям:

1. **Поле `last_data` в `amr_comm_state`**:
   * В таблице есть поля `tm`, `stm`, `logtm`, `synctm`. Какое именно из них содержит время получения последних *данных* (показаний), которое нужно сравнивать с временным окном (`star_thour` ... `end_hour`)?
2. **Связь `amr_comm_state` и ТУ**:
   * Поле `n` в `amr_comm_state` — это гарантированно `n_sh` (ID счётчика)
